[package]
name = "declint-lsp"
version = "1.0.0"
edition = "2021"
description = "LSP server for declint: publishes regex rule violations as diagnostics via increparse-lsp."
license = "MIT OR Apache-2.0"
repository = "https://github.com/thyrgle/declint"
readme = "../../README.md"
keywords = ["lint", "lsp", "language-server", "diagnostics"]
categories = ["development-tools"]

[dependencies]
declint-core = { path = "../declint-core", version = "1.0.0" }
increparse = "0.1.0"
increparse-lsp = "0.3.0"
lsp-types = "0.97"

[dev-dependencies]
declint-lua = { path = "../declint-lua", version = "1.0.0" }
